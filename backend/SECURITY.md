# Security policy

Do not commit secrets, database exports, access tokens, or production credentials. Store local values only in `.env`; use `.env.example` for safe placeholders.

If you discover a vulnerability, report it privately to the repository owner instead of opening a public issue with exploit details.
